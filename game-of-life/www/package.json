{
	"name": "game-of-life-www",
	"version": "1.0.0",
	"description": "Conway's Game of Life implemented in Rust and WebAssembly",
	"main": "index.js",
	"scripts": {
		"build": "wasm-pack build --target web --out-dir www/pkg ../",
		"serve": "python3 -m http.server 8000",
		"start": "npm run build && npm run serve"
	},
	"keywords": ["webassembly", "wasm", "rust", "game-of-life", "conway"],
	"author": "Your Name",
	"license": "MIT",
	"devDependencies": {
		"wasm-pack": "^0.12.0"
	}
}
